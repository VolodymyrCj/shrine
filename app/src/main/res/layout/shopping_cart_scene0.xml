<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/headerShoppingCart"
    android:layout_width="match_parent"
    android:layout_height="@dimen/shopping_cart_header_height"
    android:background="@drawable/shr_shopping_cart_background_shape"
    android:padding="0dp">

    <ImageView
        android:id="@+id/ivMainShoppingCart"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:padding="12dp"
        android:layout_margin="6dp"
        android:src="@drawable/shopping_cart_full" />
</FrameLayout>